group:
  person_chris:
    name: "Chris"
    entities:
      - device_tracker.chris_pixel

  person_karine:
    name: "Karine"
    entities:
      - device_tracker.karine_pixel

  household:
    name: "Household"
    icon: "mdi:account-multiple"
    entities:
      # Not using person groups because we lose the Gravatar config and groups support only icon.
      - device_tracker.chris_pixel
      - device_tracker.karine_pixel
