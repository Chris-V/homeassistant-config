# Some workaround to ensure the HS-WD100+ devices status is correctly reported

zwave:
  autoheal: False
  device_config:
    light.basement_bathroom_vanity:
      refresh_value: True
    light.basement_hallway:
      refresh_value: True
    light.dining_room:
      refresh_value: True
    light.lounge:
      refresh_value: True
    light.main_bathroom_spots:
      refresh_value: True
    light.office:
      refresh_value: True

automation:
  - alias: Notify Z-Wave network ready
    trigger:
      - event_type: zwave.network_ready
        platform: event
    action:
      - service: notify.admin
        data:
          message: Z-Wave network is ready.
          data:
            tag: zwave-status
  - alias: Notify Z-Wave network complete
    trigger:
      - event_type: zwave.network_complete
        platform: event
    action:
      - service: notify.admin
        data:
          message: Z-Wave network is complete.
          data:
            tag: zwave-status