homeassistant:
  customize:
    scene.sunset:
      friendly_name: "Sunset"
    automation.sunset_ambiance:
      friendly_name: "Turn on ambiance lights at sunset"

automation:
  - alias: sunset_ambiance
    trigger:
      - platform: sun
        event: "sunset"
      - platform: state
        entity_id: "group.household"
        to: "home"
    condition:
      condition: and
      conditions:
        - condition: time
          before: "00:00:00"
        - condition: sun
          after: "sunset"
        - condition: state
          entity_id: "group.household"
          state: "home"
    action:
      service: scene.turn_on
      entity_id: "scene.sunset"

scene:
  - name: sunset
    entities:
      light.master_bedroom_bedside_lights:
        state: "on"
        brightness: 1
        rgb_color: [21, 8, 0]
      light.outdoor_lights:
        state: "on"
      switch.living_room_ambiance: "on"
