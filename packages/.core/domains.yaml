include:
  domains:
    - device_tracker
    - input_boolean
    - input_datetime
    - input_number
    - input_select
    - input_text
    - light
    - media_player
    - sensor
    - sun
    - switch
  entities:
    - group.household
exclude:
  entities:
    - sensor.date
    - sensor.since_last_boot
    - sensor.time
