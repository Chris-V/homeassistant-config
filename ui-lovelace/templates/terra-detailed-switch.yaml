variables:
  prefix:

hold_action:
  action: more-info
  entity: "[[[ return `sensor.${variables.prefix}_power` ]]]"
styles:
  name:
    - font-size: 14px
    - padding: 0px 5px
  label:
    - color: "#80828A"
    - font-size: 12px
    - padding: 0px 5px
show_label: true
label: >
  [[[
    return Math.round(states[`sensor.${variables.prefix}_power`].state)
        + ' '
        + states[`sensor.${variables.prefix}_power`].attributes['unit_of_measurement']
        + ' ⁓ '
        + Math.round(states[`sensor.${variables.prefix}_energy`].state)
        + ' '
        + states[`sensor.${variables.prefix}_energy`].attributes['unit_of_measurement']
  ]]]
